/**
 * Type surface for the Node backfill script, so tests can import the storage-key
 * sanitiser and assert it stays backward compatible with already-stored objects.
 */
export declare function sanitiseAdvisoryKey(advisoryId: unknown): string;
